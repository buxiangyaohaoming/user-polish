# user-polish(润色插件) 安装教程与注意事项

> 适用版本:SillyTavern 1.12.0 及以上(建议使用最新版)。插件为纯前端扩展,零核心代码改动,卸载只需删除文件夹。

## 一、安装前准备

1. 一个可用的 **SillyTavern** 服务端(1.12.0+)
2. **可用的 AI API**——二选一即可:
   - 酒馆已正常连接任意 API(插件默认"使用酒馆当前 API")
   - 或准备一个 OpenAI 兼容接口(DeepSeek / OpenAI / 各类代理),在插件配置中填入

## 二、安装方法

### 方法 A:扩展面板安装(推荐,仓库发布后可用)

1. 打开酒馆 → 左侧导航 → **扩展(Extensions)** 面板
2. 点击 **安装扩展(Install Extension)** → 输入仓库地址:
   `https://github.com/buxiangyaohaoming/user-polish`
3. 点击安装 → 等待完成

### 方法 B:手动安装

1. 下载 `user-polish` 文件夹(或 `git clone`)
2. 将整个文件夹放入酒馆的扩展目录:
   ```
   data/<用户名>/extensions/user-polish/
   ```
   - Windows 示例:`E:\SillyTavern\data\default-user\extensions\user-polish\`
   - 确认目录结构正确:
     ```
     user-polish/
     ├── manifest.json
     ├── index.js
     └── style.css
     ```
3. **重启酒馆服务端**(或扩展面板中"重新扫描"扩展)

### 方法 C:手机端

手机通过浏览器访问同一酒馆地址(局域网 `http://电脑IP:8000`)即可使用,无需单独安装;首次进入需在手机浏览器执行一次强刷(清除缓存或隐身模式)。

## 三、启用与验证

1. 重启酒馆后,进入 **设置 → 扩展(Extensions)** → 找到 **润色插件**,确认状态为**已启用**(新装扩展默认启用)
2. 浏览器 **Ctrl+F5 强制刷新**(重要!浏览器缓存可能导致看不到新扩展)
3. 验证是否加载成功:
   - 按 F12 打开控制台 → 出现 `[润色插件] 已加载`,无红色报错
   - 输入框左侧**魔法棒**菜单中出现 3 项:**润色输入框 / 更新画像 / 润色插件配置**
4. 至此安装完成,可进入"快速开始"。

## 四、快速开始

1. **更新画像**:先与角色对话几条 → 魔法棒 → **更新画像**
   - 进阶:在配置界面 → 偏好画像区块,可勾选指定楼层、粘贴喜欢的段落、选择更新范围
2. **润色**:在输入框输入文字 → 魔法棒 → **润色输入框** → 弹出确认框 → 确认
   - 确认框可临时输入参考内容,或选择某条 AI 发言作为风格参考
3. **查看/恢复**:魔法棒 → **润色插件配置** → 备份区,可恢复原文 / 重新润色 / 删除
4. **自定义**:配置弹窗中修改润色模板、配置 API 预设、开启自动更新

## 五、注意事项

### 安全

1. **API 密钥为明文存储**:预设的 API Key 保存在酒馆 `settings.json` 中,并随登录账号同步到所有设备。**请勿在共享设备上保存密钥**;若仅用"当前 API"路径,则无需填写任何密钥。
2. **备份区内容**包含润色前后的原文,随酒馆设置文件保存;删除聊天不会自动清理备份。

### 使用

3. **画像为空时润色仍可用**(按通用风格润色),但效果一般——建议先更新画像。
4. **指定楼层模式**:开启后画像只从勾选的楼层提取;未选任何楼层时更新画像会提示"未选择任何楼层"。
5. **AI 发言楼层**仅作为语境/风格参考,用户发言才是画像分析重点。
6. **自动更新默认关闭**:开启后每累计 N 条新用户消息触发一次静默画像更新;注意它会同时更新全局画像与当前会话画像(后台无界面选择)。
7. **切对话不影响全局画像**;会话画像、楼层勾选、喜欢的段落均随聊天记录保存。

### 网络与兼容

8. **预设接口跨域(CORS)**:浏览器直连第三方 API 可能被拦截,报"润色失败: Failed to fetch"。解决:
   - 预设中勾选 **"经 ST 代理转发"**(需在酒馆 `config.yaml` 中开启 `enableCorsProxy`)
   - 或使用支持 CORS 的 API 服务
9. **流式请求**:预设 API 默认走流式(实时进度条);接口不支持流式时会自动回退普通请求,不影响功能。
10. **扩展不显示/不生效时**,按顺序排查:
    - 确认文件夹名、三个文件齐全(见方法 B 第 2 步)
    - 确认服务端已重启
    - 浏览器 Ctrl+F5 强刷
    - F12 控制台第一条报错若为 `/scripts/script.js 404` 之类,说明酒馆版本过旧或导入路径不兼容,请升级酒馆

### 已知限制

11. 使用酒馆"当前 API"润色时无流式进度(核心 API 不提供流回调),进度条为扫描动画;配置预设 API 后即为实时进度。
12. 楼层列表仅显示**最近 150 条**消息,更早的楼层无法通过界面勾选。

## 六、常见问题(FAQ)

| 问题 | 解决 |
|---|---|
| 点魔法棒没有新项 | Ctrl+F5 强刷;确认扩展已启用;F12 看报错 |
| 润色失败: Failed to fetch | 预设接口跨域 → 开启"经 ST 代理转发" |
| 更新画像提示"还没有用户消息" | 先发几条消息,或启用指定楼层并勾选 |
| 润色结果与预期不符 | 更新画像;在确认框输入参考内容;调整模板 |
| 想撤销润色 | 配置 → 备份区 → 恢复原文 |
| 手机浏览器看不到插件 | 手机端强刷(清缓存),确认与电脑同一网络 |
| 卸载方法 | 删除 `data/<用户名>/extensions/user-polish/` 文件夹,重启酒馆即可(设置数据保留在 settings.json,如需彻底清除可手动删除 `extension_settings.userPolish` 与 `chat_metadata.userPolish`) |
